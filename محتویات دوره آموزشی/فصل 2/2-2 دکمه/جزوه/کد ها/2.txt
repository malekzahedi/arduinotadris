#define buttonPin 2
bool state ;
void setup() { 
Serial.begin(9600);
pinMode(buttonPin, INPUT); 
} 
void loop() { 
state = digitalRead(buttonPin); 
// تاخیر برای اجتناب از زمان بونس
delay(10); 
Serial.println(state);
}
